# Input Data

Place the shared de-identified manuscript input table here before running the workflow.

Expected file name:

- `complete_dataset.csv`

This file should contain the complete pre-partition dataset with the original 6-class `BB_rating` labels and the metadata columns required by the scripts.
